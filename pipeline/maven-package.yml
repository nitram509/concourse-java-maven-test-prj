platform: linux

image: docker:///maven#3.3.3

inputs:
  - name: git-repo

outputs:
  - name: jar-file
    path: git-repo/target

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]

